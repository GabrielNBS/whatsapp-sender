Desktop distribution assets live here.

- `template.db` is generated by `npm run desktop:prepare-db`
- `chrome/` is populated by `npm run desktop:install-browser`

These files are not committed so test builds always use fresh generated assets.
